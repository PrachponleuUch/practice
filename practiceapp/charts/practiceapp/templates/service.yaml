apiVersion: v1
kind: Service
metadata:
  name: {{ include "practiceapp.fullname" . }}
  labels:
    {{- include "practiceapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "practiceapp.selectorLabels" . | nindent 4 }}
